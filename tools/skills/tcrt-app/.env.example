# Copy this file to ".env" next to SKILL.md in a runtime skill directory.
# One KEY=VALUE per line. Do not add inline comments after values.
# The real .env file must never be committed.

TCRT_BASE_URL=http://127.0.0.1:9911
TCRT_APP_TOKEN=tcrt_app_REPLACE_WITH_YOUR_RAW_TOKEN
