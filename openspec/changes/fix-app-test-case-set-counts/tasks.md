## 1. App Token read model

- [x] 1.1 Compute team-scoped grouped Test Case Set counts in the App Token test case read route and include them in `sets[]`.

## 2. Regression coverage and client guidance

- [x] 2.1 Add App Token API regression coverage for populated and empty sets, including a filtered request.
- [x] 2.2 Document the distinction between `sets[].test_case_count` and filtered `page.total` in the tcrt-app API reference and usage guide.

## 3. Verification

- [x] 3.1 Run the affected App Token read tests, relevant lint, and strict OpenSpec validation.
